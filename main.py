from src.data_preprocessing import load_data, clean_data
# from src.train_model import model # Currently this script will run on import, which is typical for this kind of setup but maybe not ideal in a real project structure. To avoid executing everything during import, we just print the pipeline start.

print("Pipeline running...")
